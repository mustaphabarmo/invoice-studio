import { createFileRoute, Link, useNavigate } from "@tanstack/react-router";
import { FileText, Loader2 } from "lucide-react";
import { useState } from "react";
import { authenticate } from "@/lib/api";
export const Route=createFileRoute("/login")({component:Login});
function Login(){const nav=useNavigate(),[busy,setBusy]=useState(false),[error,setError]=useState(""); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const data=new FormData(e.currentTarget);try{await authenticate("/login",{email:String(data.get("email")),password:String(data.get("password"))});await nav({to:"/dashboard"})}catch(e){setError(e instanceof Error?e.message:"Unable to log in") }finally{setBusy(false)}} return <AuthShell title="Welcome back" subtitle="Log in to continue to your invoice workspace."><form onSubmit={submit}>{error&&<div className="auth-error">{error}</div>}<label>Email address<input name="email" type="email" autoComplete="email" required placeholder="you@company.com"/></label><label>Password<input name="password" type="password" autoComplete="current-password" required placeholder="Enter your password"/></label><button disabled={busy}>{busy?<Loader2 className="spin"/>:"Log in to Invoice Studio"}</button></form><p>New to Invoice Studio? <Link to="/register">Create an account</Link></p></AuthShell>}
export function AuthShell({title,subtitle,children}:{title:string;subtitle:string;children:React.ReactNode}){return <div className="auth-page"><Link to="/" className="public-logo"><span><FileText/></span>Invoice Studio</Link><div className="auth-card"><div className="auth-intro"><small>INVOICE STUDIO</small><h1>{title}</h1><p>{subtitle}</p></div>{children}</div><small>Professional invoices, thoughtfully made.</small></div>}
