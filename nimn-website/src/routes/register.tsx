import { createFileRoute, Link, useNavigate } from "@tanstack/react-router";
import { Loader2 } from "lucide-react"; import { useState } from "react"; import { authenticate } from "@/lib/api"; import { AuthShell } from "./login";
export const Route=createFileRoute("/register")({component:Register});
function Register(){const nav=useNavigate(),[busy,setBusy]=useState(false),[error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const d=new FormData(e.currentTarget),password=String(d.get("password"));try{await authenticate("/register",{first_name:String(d.get("first_name")),last_name:String(d.get("last_name")),email:String(d.get("email")),password,password_confirmation:password});await nav({to:"/dashboard"})}catch(e){setError(e instanceof Error?e.message:"Unable to create account")}finally{setBusy(false)}}return <AuthShell title="Create your workspace" subtitle="Everything you need to create, brand, and organize invoices."><form onSubmit={submit}>{error&&<div className="auth-error">{error}</div>}<div className="auth-row"><label>First name<input name="first_name" required/></label><label>Last name<input name="last_name" required/></label></div><label>Work email<input name="email" type="email" required placeholder="you@company.com"/></label><label>Password<input name="password" type="password" minLength={8} required placeholder="At least 8 characters"/></label><button disabled={busy}>{busy?<Loader2 className="spin"/>:"Create free account"}</button></form><p>Already have an account? <Link to="/login">Log in</Link></p></AuthShell>}
